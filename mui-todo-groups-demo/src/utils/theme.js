import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    h1: {
      fontSize: 50,
    },
    h2: {
      fontSize: 40,
    },
    h3: {
      fontSize: 30,
    },
  },
});

export default theme;
